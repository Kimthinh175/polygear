* [ ] [ ]
  1. [ ]

* **Nhánh:** `<tên>/<type>/<mô-tả>` (Ví dụ: `khanh/feat/base-service`)
* **Commit:** `<type>(<scope>): <mô-tả>` (Ví dụ: `feat(base): add crud logic`)
* **Type hợp lệ:** `feat`, `fix`, `docs`, `refactor`, `test`, `chore`.
* **Scope hợp lệ (Tương ứng với 14 module QLSK):**
  * **Modules chính:**
    * Front-end: `home`, `catalog`, `account`, `checkout`, `AISuggestion`
    * Back-end:
  * **Modules Attendance:** `event-attendance-code`, `event-attendance-log`, `event-lecturer`, `event-end`.
  * **Hệ thống/Chung:** `common`, `core`, `config`, `auth`.
